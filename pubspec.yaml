name: my_flutter_app
description: Eine einfache Flutter-Anwendung.

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0 <4.0.0"  # Stellt sicher, dass die Flutter SDK kompatibel ist

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0  # Aktualisierte Version der http-Bibliothek
  cupertino_icons: ^1.0.2  # Falls benötigt

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
